apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-ui-to-api
  namespace: demo
spec:
  podSelector:
    matchExpressions:
      - key: role
        operator: In
        values:
          - back-end-api
          - admin-back-end-api
  ingress:
    - from:
        - podSelector:
            matchLabels:
              role: front-end
        - podSelector:
            matchLabels:
              role: admin-front-end
  policyTypes:
    - Ingress